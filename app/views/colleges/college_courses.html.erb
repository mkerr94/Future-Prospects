<%= render 'college_header' %>

<h3 class="subheading">All Courses</h3>

<br>

<div class="content-container">
  <table class="table">
    <thead>
      <tr>
        <th>Title</th>
        <th>Categories</th>
        <th>Applications</th>
      </tr>
    </thead>
      <tbody>
        <% @college_courses.each do |course| %>
          <tr>
            <td><%= link_to "#{course.title}", course_path(course) %></td>
            <td>
              <% course.categories.each do |cat| %>
                <%= link_to cat.name, category_path(cat) %>
              <% end %>
            </td>
            <td><%= link_to course.course_applications.count, {:controller => "courses", :action => "course_applications", :course => course.id } %></td>
          </tr>
        <% end %>
      </tbody>
  </table>
</div>
